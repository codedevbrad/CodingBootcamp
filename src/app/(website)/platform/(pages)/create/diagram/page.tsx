import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Diagram Creator | ThecodeBootcamp",
  description: "Student platform",
}

export default async function DiagramCreation ( ) {
    return (
        <div>
            Create a diagram
        </div>
    )
}