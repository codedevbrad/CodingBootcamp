import { animatedCodeRegistry } from "./animatedCode/package/package.registry";
import { quizRegistry } from "./quiz/package/quiz.registry"

export const blockRegistry = {
    quiz: quizRegistry,
    animatedCode: animatedCodeRegistry,
};
